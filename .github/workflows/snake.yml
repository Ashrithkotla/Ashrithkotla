name: Generate Snake

on:
workflow_dispatch:

jobs:
snake:
runs-on: ubuntu-latest

```
steps:
  - uses: Platane/snk@v3
    with:
      github_user_name: Ashrith-kotla
      outputs: dist/github-contribution-grid-snake.svg
```
